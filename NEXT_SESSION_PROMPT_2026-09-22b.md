Continue ProdCal (workshop week; Tue 22 Sep, day 2 live) in /home/exedev/prodcal. Read AGENTS.md, docs/CONTEXT-HYGIENE.md, then only addendum 23 of docs/reviews/SESSION-HANDOFF-2026-09-18.md, scratch/run/CHECKLIST.md, and the "Build list" + "Generated Typst" + "Character styles" sections of docs/reviews/CUSTOM-STYLES-MARKERS-2026-09-22.md.

First action: restart the runpage on this conversation's id (tmux kill-session -t runpage; tmux new-session -d -s runpage -c /home/exedev/prodcal "RUNPAGE_CHAT_CONV=$SHELLEY_CONVERSATION_ID scripts/run-page.sh"). Punch list: https://jdbbs.exe.xyz:8766/. Triage inbox items Hotfix-now vs After-workshop; answer on items via POST localhost:8766/note {"id","text","who":"shelley"}. One stream at a time: finish the 5.25 verification before anything else; hotfix requests from Jenna are done between slices, not in parallel.

Freeze on (hotfix-only, smoke only on mcheck = project 17, never pinstitute = 22), with Jenna's exception for 5.25 + 5.26 today. Before every make build && sudo systemctl restart prodcal: sqlite3 db.sqlite3 "select count(*) from books where status='converting'" must be 0. Template/filter/marker scripts are read from disk per build (no restart); Go and srv/static changes need the restart (static is embedded). Version shows on the factory strip as v MMDD.hash (/api/version).

Next action, in order:
1. Verify slices 1–3 on mcheck: verse2/verse3/break2 are already declared on project 17's transmittal. Sign in at http://localhost:8000/mcheck/book-001/factory/ (password in scratch/idx-smoke/pw), upload scratch/verse/in.docx (Toby's file: 508 Body A paras, markers verse×381 verse2×21 verse3×4 break×48 break2×3), Inspect, proof, then pdftoppm a few pages + read_image: verse2 lines sit 1.5 em further in than verse, verse3 3 em, break2 leaves a visibly larger gap. Check the generated book.typ/config has `#let verse2(content) = poem(indent: 1, content)`. If `sc(...)` is out of scope for character parents, switch to `smallcaps`.
2. Slice 4: EPUB classes (docx-to-epub.lua + epub-styles.css) and Word template basedOn/indent (generate-word-template.py reads based_on/indent from the spec JSON).
3. Slice 5: inline character markers `Type [[code]]ls -la[[/code]] to…` in apply-style-markers.py + detect-edge-cases.py; friendlier unresolved wording ("we don't know that one yet — add it under Custom styles…"); TestApplyStyleMarkersScript cases. Fotis's file leaked [[ITAL]]/[[BOLD]] into book.typ — that's the test case.
4. Slice 6: admin.html tsRenderCustomStyles (~L2725) gains Based on / Indent / Space before + resolved line. Slice 7: /factory#bring, /workshop#bring (in /home/exedev/jdbbs-public, publish on save), WORD-FREE-AUTHORING note.
5. Fotis review: check whether docs/reviews/FOTIS-HELO-WORD-REVIEW-2026-09-22.md exists (subagent fotis-review, conv cFLT76L, may still finish). If yes, read only its summary, commit, tell Jenna on the punch list. If not, re-dispatch with brief scratch/fotis/BRIEF.md on a cheaper model, instructing it to write the file skeleton first.

Floor watch: Sam (31) green; Toby (29) no retry; hermescorp/Fotis (30, now named "helo word") book 57 failed on our `#let break(content)` bug — fixed in 9258e56, he can retry; Ellen (33); a new participant expected. Failed builds keep /tmp/prodcal-failed/book-N/. Build QC mail to Jenna is on (PRODCAL_BUILD_QC_EMAIL in .env; remove after the workshop).

Unchanged: talk deck /home/exedev/jdbbs-public/2026-pi-symposium/og-protocols.html; 0.9; 5.22 on her yes; 5.23 parked; 5.24 + 5.27 tone pass after workshop; 0.34 onboarding parked. Push with git push git@github.com:djinna/jdbbs.git main. Commit before returning to Jenna; write addendum 24 + this file's successor at ~50–65 % context.
