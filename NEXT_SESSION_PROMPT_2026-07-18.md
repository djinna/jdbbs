# Session handoff — PI Editorial Stylesheet: theme unification, ledger redesign, scope tag, docs

You are continuing work on the **PI Editorial Stylesheet review tool** in the
`prodcal` app (Go + SQLite, module `srv.exe.dev`, repo `/home/exedev/prodcal`).
It is served on the **default port 8000** via the exe.dev proxy and reachable at
`https://jdbbs.exe.xyz/stylesheet/` (editor) and `/stylesheet/authors`
(read-only authors' view). exe.dev injects `X-ExeDev-Email` / `X-ExeDev-UserID`
headers for authenticated users; writes are allowlisted (see below).

Service runs under systemd unit `prodcal` (`sudo systemctl restart prodcal`,
`journalctl -u prodcal`). WorkingDirectory `/home/exedev/prodcal`, db file
`db.sqlite3`. Build with `make build` (= `go build -o prodcal ./cmd/srv`).
Last commit at handoff: `aae8918` "Add PI editorial stylesheet review tool".
Tree is clean.

## Current state / architecture (already built, working)
- Migration `db/migrations/019-stylesheet.sql`: tables `stylesheet_items`
  (one row per rule/prose item) and `stylesheet_edits` (side-by-side proposed
  edits held pending until accepted).
- Seeded (134 items) on first API hit from embedded `srv/stylesheet_seed.json`
  (generated by `db/seed/parse_stylesheet.py` from
  `editorial/Zoothesia/STYLESHEET.md` — that source lives in the SEPARATE repo
  `/home/exedev/book-production`). Seeding is idempotent (skips if rows exist).
- Backend: `srv/stylesheet.go` — API + page + export routes, registered via
  `s.registerStylesheetRoutes(mux)` in `srv/server.go` BEFORE the SPA catch-all.
  Item/Edit JSON shapes + endpoints documented in `docs-stylesheet-SPEC.md`.
- Frontend (vanilla JS, no build step): `srv/static/stylesheet/{index.html,
  authors.html,app.js,style.css}`. Editor cards show status badges, accept/
  reject, author-facing star, inline edit -> pending edit with word-level diff.
  `?demo=1` loads sample data for offline visual testing.
- Write allowlist (email -> display name), in `srv/stylesheet.go` `stylesheetEditors`:
  `j@djinna.com` -> "JD (Publisher)", `editor@protocolized.io` -> "James Langdon (PI Editor)".
- Exports (regenerated from DB, DB is canonical): `/stylesheet/export.html`,
  `/stylesheet/export.md`, `/stylesheet/authors.html`, `/stylesheet/authors.md`
  (authors sheet = items with status=accepted AND author_facing=true).

## Canonical jdbbs "chrome" to reuse
`srv/static/theme.css` + `srv/static/theme.js` define the SITE-WIDE design
system: `:root` tokens (cyan `--accent:#007699`, hairline "ledger" styling,
zero border-radius, no shadows — see the design-system comment block in
theme.css and `docs/DESIGN-SYSTEM.md` if present), `html.dark` palette,
`html[data-font=...]` families, and the `.theme-bar` font-selector +
dark-toggle component. `theme.js` exposes `window.JdbbTheme.mount(el)` which
injects the full upper-right control (font dropdown grouped Mono/Sans + Serif,
8 fonts: jetbrains/martian/plex/geist/literata/ibm-serif/source-serif/
newsreader, plus dark toggle). All fonts load from ONE Google Fonts <link>
(see `srv/static/transmittal.html` for the canonical link tag). Storage key
`prodcal-theme-v1`.

The stylesheet page currently ships its OWN bespoke dark-only toggle and a
duplicated `:root`/dark/theme-bar/font block in its `style.css` — this is the
divergence to remove.

## TASKS (do all three, then commit)

### 1. Unify theming + full ledger redesign of the stylesheet page
Decision from the user: go ALL THE WAY to the prodcal **ledger look**
(hairline rules + whitespace + aligned columns, NO cards, zero border-radius,
zero shadows, mono bracketed tags instead of pills, one filled accent button
per page, all other actions as underlined text-links). And adopt the shared
chrome so this page matches the rest of jdbbs.

Concretely:
- Have `index.html` and `authors.html` `<link>` the canonical
  `/static/theme.css` and load `/static/theme.js`, and mount the real control:
  give them a `<div id="theme-bar"></div>` and call
  `JdbbTheme.mount(document.getElementById('theme-bar'))`. Remove the page's
  hand-rolled `.theme-bar`/`.dark-btn` markup and the inline pre-hydration
  script's font list if the canonical apply() covers it (keep a tiny no-flash
  guard only if needed).
- DELETE from `srv/static/stylesheet/style.css` the duplicated design tokens:
  its own `:root`, `html.dark`, `.theme-bar`, `.dark-btn`, and the
  `html[data-font=...]` overrides — inherit them from theme.css instead.
- Remove the self-contained dark-mode logic in `app.js` (the `TKEY`/`loadDark`/
  `saveDark`/`applyDark`/`initDark` block and its call) now that JdbbTheme owns
  dark + font state. Make sure nothing else in app.js depended on `#dark-btn`.
- Rewrite the REMAINING page components (section headers, item rows, status
  badges, author-facing toggle, filters, inline editor, the word-level diff
  view, toasts, exports bar) to the ledger aesthetic using the SHARED token
  names (`--accent`, `--text`, `--text-secondary`, `--text-muted`, `--border`,
  `--border-strong`, `--surface`, `--green`, `--red`, `--mono`, `--body`, etc.
  from theme.css). Replace cards with hairline-separated rows; pills with mono
  bracketed `.tag` text; buttons with `.link-action` text-links except at most
  one `.btn-fill` per page. Reuse the ledger primitives already defined in
  theme.css (`.kicker`, `.tag`, `.btn-fill`, `.link-action`, `.input-bare`,
  `hr.rule`, `hr.rule-strong`) rather than reinventing.
- DO NOT edit `theme.css`/`theme.js` themselves (they are shared by admin,
  client, transmittal, landing — regressions there are out of scope). If you
  genuinely need a token that doesn't exist, prefer a page-local fallback var.
- The diff view keeps word-level add/remove highlighting; recolor to
  `--green`/`--red` house tokens (added green, removed red strikethrough).
- Verify in the browser at `http://localhost:8000/stylesheet/?demo=1` (editor
  controls + a pending-edit diff) and `/stylesheet/authors?demo=1`, in BOTH
  light and dark, and exercise the font dropdown so body type actually changes.
  Screenshot to confirm parity with the rest of jdbbs.

### 2. New per-item "scope" control: Zoothesia (this title) vs Universal
Add a way for editors to tag each item's SCOPE: `universal` (goes into the
org-wide PI master stylesheet) vs `zoothesia` (specific to the current title;
kept with the book's archive). This is the key data distinction that lets us
carry universal rules forward to the next titles while retiring book-specific ones.
- Schema: add a `scope` column to `stylesheet_items` via a NEW migration
  `db/migrations/020-stylesheet-scope.sql` (values `universal` | `zoothesia`,
  default `universal`). Follow the exact migration conventions in
  `db/db.go`/existing migrations, incl. the
  `INSERT OR IGNORE INTO migrations (migration_number, migration_name)
  VALUES (020, '020-stylesheet-scope');` footer.
- Backend: include `scope` in the item JSON (`ssItem`), the load query, and
  allow updating it via the existing `PATCH /api/stylesheet/items/{id}`
  handler (add an optional `scope` field alongside status/author_facing;
  validate against the two allowed values; write-gated to the allowlist).
- Frontend: a small scope control on each editor item (a two-way toggle or
  bracketed mono tag button, ledger-style) showing/settin g universal vs
  zoothesia. Show current scope as a `.tag`. Read-only for non-editors.
- Exports/filtering: add scope-aware output. At minimum, the MASTER exports
  should be filterable to `scope=universal` (the emerging org master sheet),
  while the existing Zoothesia archive view keeps everything. Suggest:
  `/stylesheet/export.md?scope=universal` (and `.html`), plus keep the full
  export as-is. The authors' sheet filter is unchanged
  (accepted AND author_facing) but should ALSO respect a scope filter if
  passed. Keep it simple; document any query params you add in
  `docs-stylesheet-SPEC.md`.
- Consider whether the on-page filter bar should gain a Universal/Zoothesia
  filter too (nice-to-have; do it if cheap).

### 3. Process documentation in /docs
Write a durable playbook so we can run this copyedit -> stylesheet pipeline on
the next couple of titles and progressively flesh out the ORG MASTER stylesheet.
Put it in the book-production repo's docs area (that's where editorial process
lives): create `/home/exedev/book-production/docs/EDITORIAL-STYLESHEET-PROCESS.md`
(check `/home/exedev/book-production/docs/` conventions first; there is a
`docs/symposium-2026/README.md` guidance file — read it and any docs/README).
It should capture, concisely and reproducibly:
- The end-to-end flow we used for Zoothesia: source .docx -> pandoc extract ->
  copyedit -> CHANGELOG/COPYEDIT per story -> STYLESHEET.md -> HTML review ->
  import into the prodcal review tool -> accept/reject/edit + tag
  author-facing + tag scope -> regenerate exports.
- How the review tool works and where it lives (URLs, allowlist, DB-canonical,
  seeding, exports), so a future operator can drive it.
- The universal-vs-title scope model and HOW the org master stylesheet
  accumulates across titles (universal items carry forward; title-specific
  items stay with each book's archive). Describe the intended workflow for
  title #2 and #3: re-seed a new title's items, promote recurring rules to
  `universal`, keep the master sheet as the growing canonical org reference.
- Pointers to the concrete artifacts: `db/seed/parse_stylesheet.py`,
  `docs-stylesheet-SPEC.md`, the migrations, and the export endpoints.
Keep it practical and skimmable (headings, short steps), not a novel.

## Wrap-up
- Rebuild (`make build`), restart (`sudo systemctl restart prodcal`), smoke-test
  the API (simulate identity with `-H X-ExeDev-Email:editor@protocolized.io`)
  and the pages; reset any test mutations to the DB so the team starts clean
  (all items `proposed`) as was done previously — clear `stylesheet_items` +
  `stylesheet_edits` + their `sqlite_sequence` rows, then re-hit the items API
  to re-seed.
- Commit the prodcal changes with a good message; commit the book-production
  doc in that repo separately. Both repos have `j@djinna.com` as author.
- exe.dev CLI commands (share, etc.) must be run by the USER from THEIR local
  machine (`ssh exe.dev ...`), never from inside the VM. No share changes are
  needed for this work (all on the default port already).
