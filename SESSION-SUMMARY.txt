Production Calendar + Manuscript Transmittal — Session Summary
2026-02-23 (session 9)
========================================

STATUS: ✅ LIVE AND PUBLIC

SESSION 10 CHANGES (17:15–17:35 UTC)
------------------------------------
Two new features: File Log + Project Journal

1. File Log — structured record of file transfers
   • New file_log table (migration 006-file-log-journal.sql)
   • Fields: direction (inbound/outbound), filename, file_type,
     sent_by, received_by, notes, transfer_date
   • API: GET/POST /api/projects/{id}/file-log,
     DELETE /api/projects/{id}/file-log/{entry}
   • Client-level: GET /api/clients/{client}/file-log
   • UI: "Files" tab in calendar with table view (date, direction
     with ↓ In / ↑ Out badges, filename, type, from→to, notes)
   • "+ Log Transfer" button → modal form with direction dropdown,
     date picker, filename, file type selector, from/to, notes

2. Project Journal — lightweight timestamped notes
   • New journal table (migration 006-file-log-journal.sql)
   • Fields: entry_type (call/decision/approval/note), content
   • API: GET/POST /api/projects/{id}/journal,
     DELETE /api/projects/{id}/journal/{entry}
   • Client-level: GET /api/clients/{client}/journal
   • UI: "Journal" tab in calendar with reverse-chronological feed
     of color-coded entries (📞 Call, ⚖️ Decision, ✅ Approval, 📝 Note)
   • "+ Add Entry" button → modal with type selector + text area

3. Client Portal Activity Section
   • New "Recent Activity" section at bottom of /{client}/ dashboard
   • Sub-tabs: 📁 Files | 📓 Journal
   • Shows recent entries across ALL client projects
   • Each entry shows project name label for cross-project visibility

New files:
   • db/migrations/006-file-log-journal.sql
   • srv/filelog.go — file log handlers
   • srv/journal.go — journal handlers + shared auth helper

PROJECT: Online shared/protected production calendar
REPO: /home/exedev/prodcal (Go web app)
PUBLIC URL: https://jdbb-prod.exe.xyz/vgr/aog/
ADMIN URL: https://jdbb-prod.exe.xyz/ (requires exe.dev login)
PASSWORD: artofgig2026

WHAT IT IS
----------
A web app built from Jenna's old .xls production schedule
(Prepr_ArtofGig.xls). Go backend, SQLite DB, vanilla JS SPA.
Password-protected, publicly accessible, runs as systemd service.

FEATURES WORKING
----------------
• 3 views: Timeline (Gantt), Table, Budget
• 31 tasks imported from the original .xls spreadsheet
• Task editing (click any row → modal with all fields)
• Password protection per project
• "Make New" — duplicates a project with:
  - New name, client slug, project slug, start date
  - All dates shifted proportionally
  - Budgets zeroed, statuses reset
• Client/project URL routing: /{client}/{project}/
  - Art of Gig lives at /vgr/aog/
  - Pattern ready for additional clients/books
• Light/dark mode toggle (☀️/🌙 button, persists in localStorage)
• Readable timeline with month labels + week tick marks
• Runs as systemd service (survives reboots)
• Admin project list at root /
• Public access configured

SESSION 9 CHANGES (19:50–20:55 UTC)
-----------------------------------
Six features / fixes:

1. Project Snapshot Email (📧 Email Snapshot button)
   • POST /api/projects/{id}/snapshot/email
   • Sends comprehensive HTML email: schedule overview (stat cards),
     full task table with status badges and overdue highlights,
     budget summary with variance, transmittal status with checklist count
   • Plain text fallback included
   • Recipient picker modal (same as transmittal email)
   • Tested end-to-end: delivered to jdbb@agentmail.to ✓

2. Client Login Portals
   • New clients table (migration 005-clients.sql)
   • Client portal at /{client}/ with password-protected login
   • VGR client (artofgig2026): Art of Gig + tweetbook
   • SW client (willwrite2026): The Digital Garden (faux book, 11 tasks)
   • Client cookie grants access to ALL client projects without re-auth
   • Auth cascade: project token → client cookie → exe.dev admin
   • API: GET/POST /api/clients/{client}, /api/clients/{client}/verify,
     /api/clients/{client}/projects

3. Admin Auth Fix
   • /admin/ now redirects to /__exe.dev/login instead of returning 403
   • Uses exe.dev login flow (sets X-ExeDev-UserID header on return)
   • Landing page admin link also goes through login flow

4. Email Modal Post-Send UX
   • After successful send, Cancel + Send buttons replaced with single "✓ Done"
   • Applied to both snapshot and transmittal email modals

5. Landing Page (root URL security)
   • / now shows branded landing page instead of project list
   • Logged-in exe.dev users auto-redirect to /admin/
   • Client portal links (VGR, SW) instead of direct project links

6. Status Toggle Shortcut
   • Click status badges in Timeline or Table view to cycle:
     pending → active → done → pending
   • Marking done auto-sets actual_done date to today
   • Hover effect: scale + accent ring
   • Works without opening edit modal

Also fixed:
   • h() function in app.js: proper handling of boolean attributes
     (checked, disabled, selected)

CLIENT CREDENTIALS
------------------
VGR (Venkatesh Rao): artofgig2026  → /vgr/
  • Art of Gig (/vgr/aog/) — 31 tasks
  • tweetbook (/vgr/tweets/) — 31 tasks
SW (Sarah Williams): willwrite2026  → /sw/
  • The Digital Garden (/sw/digarden/) — 11 tasks

NOTE: Live DB is at /home/exedev/db.sqlite3 (not prodcal/db.sqlite3)
  WorkingDirectory in srv.service = /home/exedev/

SESSION 8 CHANGES (19:22–19:45 UTC)
-----------------------------------
Email notifications via AgentMail:
• NEW: srv/email.go — AgentMail API client + HTML/text email builder
• NEW: POST /api/projects/{id}/transmittal/email — send transmittal summary
• NEW: GET /api/email/status — reports if email is configured
• NEW: ✉️ Email button in transmittal toolbar → modal with recipient picker
  - Default recipients: jdbb@agentmail.to + j@djinna.com
  - Custom "Other" field for additional recipients
  - "Mark Final" auto-opens email modal
• Config: AGENTMAIL_API_KEY + AGENTMAIL_INBOX_ID in .env
• systemd EnvironmentFile directive for .env loading
• Tested end-to-end: email delivered to jdbb@agentmail.to ✓

Notes versioning:
• Notes files now timestamped: notes-YYYY-MM-DD-HHMM-sN.html
• /static/notes.html is now a version index page
• Each session snapshot preserved permanently

SESSION 7 CHANGES (19:03–19:22 UTC)
-----------------------------------
Infra prep (section 5 checklist):
• NEW: /healthz endpoint — DB readability check, returns {status: ok/error}
• NEW: Automated daily SQLite backup
  - scripts/backup-db.sh: sqlite3 .backup + gzip, 7-day retention
  - Cron job at 3 AM daily → ~/backups/
• Go version pinned at 1.26.0 in go.mod (already was)
• NEW: Dockerfile (multi-stage, golang:1.26 → debian:bookworm-slim)
• NEW: DEPLOY.md — full deployment/operations guide
  (build, migrations, seeding, backup/restore, Docker, monitoring)

Admin dashboard:
• NEW: Admin dashboard at /admin/ (exe.dev login required)
  - Shows all projects with task counts, completion %, auth & transmittal status
  - Light/dark mode toggle (shared localStorage with calendar/transmittal)
  - Health indicator in header
  - Auto-refreshes every 30s
  - API: GET /api/admin/projects (requires X-ExeDev-UserID header)

Housekeeping:
• Set password on tweetbook project (artofgig2026)
• Both projects now show 🔒 Protected in dashboard

EMAIL INTEGRATION — LIVE ✓
--------------------------
AgentMail inbox: jdbb@agentmail.to
API key in /home/exedev/prodcal/.env (chmod 600, gitignored)
Implemented:
• ✓ Email summary when transmittal is marked Final (with recipient picker)
• ✓ On-demand email via ✉️ button in transmittal toolbar
Planned:
• On-demand PDF generation/email of full project snapshot
  (calendar views, transmittal form, budget summary)
• Possibly: scheduled status digest emails

SESSION 6 CHANGES
-----------------
• NEW: Manuscript Transmittal form app
  - Lives at /{client}/{project}/transmittal/
  - Digitizes the classic InDesign manuscript transmittal form
  - Single JSON blob storage in SQLite (transmittals table)
  - Auto-creates blank form on first visit
  - Auto-saves every field change (debounced 600ms)
  - Two-column layout matching original form structure:
    Left: Book Info, Production, Manuscript Checklist (15 front-matter
    components + 6 back-matter items), Illustrations table
    Right: Permissions & Consents, Page IV (Copyright), Subrights
    (with n/a strikethrough toggle), Editing, Book Design (trim
    radio buttons), Cover (JDBB/Publisher front/spine/back),
    Files & Delivery, Page Proofs (dynamic reviewer list),
    Other Instructions
  - Progress bar showing form completion %
  - Draft/Final status toggle
  - Print button (CSS @media print styles)
  - Calendar ↔ Transmittal cross-link buttons
  - Same password protection as production calendar
  - Seeded with More Than a Woodlot data from original InDesign form

SESSION 5 CHANGES
-----------------
• Added light/dark mode toggle with localStorage persistence
• Fixed unreadable Gantt date headers: month labels instead of
  cramped weekly dates, pxPerDay 3→5 for wider bars
• Cleaned stale TODO items from notes.html

INFRASTRUCTURE
--------------
• exe.dev proxy: port 8000 → https://jdbb-prod.exe.xyz/
• Blink SSH configured on iOS (key: eve.dev, host: exe.dev)
• systemd service: srv.service
• Binary: make build → ./prodcal

STARTER PROMPT FOR NEXT SESSION
-------------------------------
```
This is a continuation of the production calendar + manuscript
transmittal project.

Repo: /home/exedev/prodcal
Live: https://jdbb-prod.exe.xyz/vgr/aog/ (password: artofgig2026)
Transmittal: https://jdbb-prod.exe.xyz/vgr/aog/transmittal/
Admin: https://jdbb-prod.exe.xyz/admin/ (exe.dev login)
Notes: https://jdbb-prod.exe.xyz/static/notes.html

Go backend + vanilla JS SPA, SQLite DB, systemd service.
Email: AgentMail configured (jdbb@agentmail.to), env in .env.
Session notes at /home/exedev/prodcal/SESSION-SUMMARY.txt

Tasks:

1. "Email PDF" button: generate and email a snapshot of the
   current state of a book project — calendar views, transmittal
   form, budget summary — as a well-formatted HTML email body
   (or PDF attachment). The Go backend has all the data; the
   AgentMail send endpoint is already wired in srv/email.go.

2. Root URL (https://jdbb-prod.exe.xyz/) currently serves the
   calendar SPA to anyone — it shows a project list without
   auth. Consider: should / redirect to a landing page, show
   only public-facing info, or require auth?

3. /admin/ is blocked for non-exe.dev users (returns 403).
   This is by design (X-ExeDev-UserID header). It works when
   accessed through the exe.dev proxy with a logged-in session.
   If it's blocked for YOU, check that you're logged into
   exe.dev and accessing via https://jdbb-prod.exe.xyz/admin/.

Nice-to-haves:
  • Periodic status digest email (weekly?) for all projects
  • Mobile-responsive improvements
  • Status toggle shortcut on calendar tasks
```

FIX / BUILD LIST
----------------
Admin / dashboard:
  • Add link to reports page on /admin/ dashboard

File submission / file sharing:
  • Currently using Dropbox — works fine but no visibility in app
  • At minimum: document/log file transfers in the app
    (who sent what, when, which project)
  • Stretch: build file transfer into the app
    - Inbound: .docx, .tiff, .jpg, .png, .eps
    - Outbound: .pdf, .epub
    - Could be trivial (S3/R2 upload + metadata) or nontrivial
      (preview, versioning, approval workflows)
  • Decision needed: log-only vs. actual file hosting

Calendar polish:
  • Mobile-responsive improvements (modals, gantt on small screens)
  • Polish timeline Gantt (today marker sometimes off, bar tooltips)
  • Status toggle shortcut (click status badge to cycle pending→active→done)
  • Task reordering / drag-and-drop
  • Export/print view

Transmittal polish:
  • Seed as template: when "Make New" duplicates a project, option to
    also create a blank transmittal with the default checklist
  • YAML export/import (per the Claude session notes about
    book-transmittal.yaml as single source of truth)
  • Form validation / required-field highlighting
  • Coauthors field (original has multiple authors)
  • Section collapse/expand
  • Custom checklist items (add/remove/reorder components)
  • Better mobile layout (single column works but could be tighter)

Data & workflow:
  • Test with colleague — get real feedback
  • Activity log / change history
  • Notifications / email alerts for upcoming due dates

CONVERSATION CHAIN
------------------
shared-protected-calendar (cFUDS6C) — main build session
  → shared-protected-calendar-1 (cD4RV7B) — continuation
  → shared-protected-calendar-2 (cVT6NYU) — continuation
  → shared-protected-calendar-3 — verified everything, public access
  → session 5 — light/dark mode, timeline fix
  → session 6 — manuscript transmittal form
  → session 7 — infra prep, admin dashboard
  → session 8 (current) — AgentMail email, notes versioning

KEY FILES
---------
cmd/srv/main.go              — entrypoint
srv/server.go                — HTTP handlers + routing (calendar + transmittal + email routes)
srv/transmittal.go           — transmittal API handlers (GET/PUT/versions/duplicate)
srv/email.go                 — AgentMail client + email builders (HTML + text)
srv/admin.go                 — admin dashboard handlers
srv/static/app.js            — calendar SPA frontend (~700 lines)
srv/static/transmittal.js    — transmittal SPA frontend (~960 lines)
srv/static/transmittal.css   — transmittal form + email modal styles
srv/static/transmittal.html  — transmittal shell HTML
srv/static/admin.html        — admin dashboard SPA
srv/static/style.css         — dark/light theme styles (shared)
srv/static/index.html        — calendar shell HTML
srv/static/notes.html        — notes version index
srv/static/notes-*.html      — dated session snapshots
db/migrations/001-base.sql   — schema
db/migrations/002-slugs.sql  — client/project slug columns
db/migrations/003-transmittal.sql — transmittal table
db/migrations/004-transmittal-versions.sql — version history
db/queries/visitors.sql      — sqlc queries
Makefile                     — make build → ./prodcal
srv.service                  — systemd unit file
.env                         — AgentMail API key + inbox ID (gitignored)
