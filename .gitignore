prodcal
/prodcal-local
/prodcal-app
db.sqlite3*
*.db
seed_data.json
.env
*.sqlite3
.prodcal-secret
SESSION-SUMMARY.txt
TEAM-UPDATE.txt
.hermes/

# Claude Code local state — bootstrap per CLAUDE.md instructions
.claude/
scratch/
.gstack/

# Licensed fonts — print-only, never committed (TRK-DESIGN-002).
# Repo/GitHub is a distribution channel; standard desktop licenses don't
# cover redistributing the font files. Keep README so the convention is
# documented (the /* + negation form ignores the fonts but tracks the README).
typesetting/fonts/licensed/*
!typesetting/fonts/licensed/README.md
