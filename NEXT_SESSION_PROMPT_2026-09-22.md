Continue ProdCal (workshop week; Tue 22 Sep, day 2 live) in /home/exedev/prodcal. Read AGENTS.md, docs/CONTEXT-HYGIENE.md, then only addendum 22 of docs/reviews/SESSION-HANDOFF-2026-09-18.md, scratch/run/CHECKLIST.md, and — this is the spec for today's build — docs/reviews/CUSTOM-STYLES-MARKERS-2026-09-22.md in full.

First action: restart the runpage on this conversation's id (tmux kill-session -t runpage; tmux new-session -d -s runpage -c /home/exedev/prodcal "RUNPAGE_CHAT_CONV=$SHELLEY_CONVERSATION_ID scripts/run-page.sh"). Punch list: https://jdbbs.exe.xyz:8766/. Triage inbox items Hotfix-now vs After-workshop; answer on items via POST localhost:8766/note {"id","text","who":"shelley"}.

Freeze is still on (hotfix-only, smoke only on mcheck = project 17, never pinstitute = 22) with ONE exception Jenna granted this morning: build 5.25 + 5.26 now (Tue morning, ~2 hours) because it helps people working today. Follow the "Build list" in the design note, in order: (1) spec + transmittal row `Name · Based on (required, 13 factory paragraph styles) · Indent none/1.5em/3em (only Verse, Block Quote, Code Block) · Space before as usual/3×/6× stanza gap (only Section Break) · Purpose`, values printed in the UI, resolved-form line ("verse2 → Verse, indent one level"); (2) bookspecs.go generates `#let ident(c) = poem(indent: 1, c)` etc., hand-written `typst` snippet wins, tests; (3) series-template.typ: poem(indent:), blockquote(indent:), code-block(indent:), section-break(gap:); (4) EPUB classes + Word template basedOn/indent; (5) inline character markers `Type [[code]]ls -la[[/code]] to…` in apply-style-markers.py + detect-edge-cases.py, friendlier unresolved wording ("we don't know that one yet — add it under Custom styles…"), tests; (6) admin book-spec preset select + snippet textarea; (7) /factory#bring + /workshop#bring docs. Ship in slices — (1)-(3) first, restart, verify, then the rest. Before every `make build && sudo systemctl restart prodcal`: `sqlite3 db.sqlite3 "select count(*) from books where status='converting'"` must be 0. Verify on mcheck: declare verse2 (Verse, 1 level), verse3 (Verse, 2 levels), break2 (Section Break, 3×) on project 17's transmittal, upload scratch/verse/in.docx (Toby's file, 508 Body A paras, markers verse×381 verse2×21 verse3×4 break×48 break2×3), Inspect, proof, read the PDF pages with pdftoppm + read_image. Template/filter/marker script are read from disk per build (no restart for those); Go changes need the restart.

Already live today (no action): Verse standard is roman/left/hanging (1d08665); build QC mail to Jenna on every build via PRODCAL_BUILD_QC_EMAIL in .env + /api/books/{id}/download/source + Floor [docx · pdf · epub] links (2b73cc8, 9213e2b) — switch off after the workshop by removing the .env line. Floor: Sam Khoo (31) rebuilt book 45 green; Toby (29) has not retried; hermescorp (30) book 56 has 3 Inspect highs and no build yet; Ellen (33) onboarded; a new participant is expected today — watch factory_events, and a failed build keeps its dir in /tmp/prodcal-failed/book-N/.

Otherwise unchanged: talk deck /home/exedev/jdbbs-public/2026-pi-symposium/og-protocols.html (26 screens, publishes on save; one commit per edit, push, verify at 1280×720 via keydown on document); 0.9 snags; 5.22 on her yes; 0.30 after workshop; 5.23 narrowed to poem-per-page/title opener/size, parked; 5.27 tone pass after workshop. Push with `git push git@github.com:djinna/jdbbs.git main` (the https origin has no credentials on the VM). Commit before returning to Jenna; write addendum 23 + this file's successor at ~50–65 % context.
